import {
    replaceUsers,
    REPLACE_USERS,
    getUsers,
    getUserDone,
    GET_USERS_DONE,
    getUserRequested,
    GET_USERS_REQUESTED
} from "./users.actions";

export default {
    replaceUsers,
    REPLACE_USERS,
    getUser: getUsers,
    getUserDone,
    GET_USERS_DONE,
    getUserRequested,
    GET_USERS_REQUESTED
}